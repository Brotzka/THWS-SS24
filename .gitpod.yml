tasks:
  - name: Frontend
    init: |
      cd frontend
      npm install
    command: |
      cd frontend
      npm run dev
ports:
  - name: Frontend
    description: Port 3000 for the frontend
    port: 5173
    onOpen: open-preview
